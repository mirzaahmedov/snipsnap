import { Logo } from "@app/common/components";

const Header = () => {
  return (
    <header className="w-full px-10 py-5 flex items-center justify-start">
      <Logo />
    </header>
  );
};

export { Header };
